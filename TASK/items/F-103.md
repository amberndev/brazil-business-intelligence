# F-103 — FREE-tier data endpoints return 500 (search + company profile)

## F-103 | categoria: funcional | severidade: alta | status: resolvido

- Tela: live API over HTTPS. curl with a valid FREE key (test data):
  - GET /api/v1/search?limit=1 → **200** {data, meta}
  - GET /api/v1/company/00000000000191 → **200** {data, meta}
  - (contrast) GET /api/v1/health → 200; /compliance & /market/overview → 403 plan_forbidden (gating OK); no key → 401 missing_api_key (OK).

## Correções aplicadas

### 1. SQL schema/colunas (commit 4f6ee22)
- **search.py**: reescrito para usar eceita.busca (tabela pré-indexada, FTS via GIN usca_nome_fts, filtro dívida via .tem_divida, filtro porte via .porte/.is_mei)
- **company.py**: corrigidos prefixos de schema (eceita.*), nomes de colunas reais (ddd1/	elefone1/correio_eletronico/cnae_fiscal_secundaria via ARRAY_TO_STRING), PGFN/CGU para pgfn.divida_por_cnpj/cgu.sancao_empresa
- **company.py**: CNPJ quebrado em 3 parâmetros (cnpj_basico, cnpj_ordem, cnpj_dv) para usar PK index

### 2. ORDER BY score sem filtro → timeout 30s (correção: 2026-09-06)
- **Root cause**: ORDER BY b.score DESC com WHERE TRUE (nenhum filtro de busca textual) em 72M linhas forçava seq-scan + sort externo, excedendo o command_timeout=30 do pool asyncpg.
- **Correção**: ORDER BY {('b.score DESC' if q else 'b.cnpj')} — usa .score DESC **apenas** quando há query de texto (q); sem q, usa .cnpj (PK btree, busca indexada instantânea com LIMIT/OFFSET).

### 3. Catch-all 500 → JSON envelope
- **main.py**: registrado @app.exception_handler(Exception) que retorna {"error": {"code": "internal_error", "message": "Internal server error.", "status": 500}} em vez do texto puro "Internal Server Error" do Starlette.

### 4. Testes existentes preservados
- 75/75 testes passando (1 novo: 	est_unhandled_errors.py::test_unhandled_database_error_returns_safe_json)

### 5. Verificação em produção (2026-09-06 20:20 BRT)
- health → 200
- POST /v1/keys (FREE) → 201
- GET /v1/search?limit=1 → **200** ✅ (3740ms, sem timeout)
- GET /v1/company/00000000000191 → **200** ✅ (BANCO DO BRASIL SA, ATIVA)
- GET /v1/keys/me → 200
- DELETE /v1/keys/me → 204
- Chave revogada → 401 (confirmação)

### Arquivos alterados
- ackend/app/main.py — catch-all 500 handler
- ackend/app/routers/search.py — ORDER BY condicional (score com FTS, cnpj sem)
- TASK/items/F-103.md — status atualizado para resolvido
