# F-103 — Live API exposes 9 endpoints vs 7 in contract; unauthenticated POST /v1/keys mints FREE API keys

## F-103 | categoria: funcional | severidade: média | status: aberto
- Tela: `https://brazil.ambern.dev/api/openapi.json` (200) — `paths` lists, beyond the 7 contract endpoints, three extra: `POST /v1/keys` (summary "Self-serve key generation for FREE plan (no X-API-Key auth required)"), `GET /v1/keys/me`, `DELETE /v1/keys/me`. Cross-ref `backend/app/routers/keys.py` mounted at `main.py:34`.
- Passos:
  1) `curl https://brazil.ambern.dev/api/openapi.json` and read `paths`.
  2) Count operations: health, company, company/compliance, company/shareholders, company/batch, search, market/overview (the 7 in CONTRACT.md §A) **plus** `POST /v1/keys`, `GET /v1/keys/me`, `DELETE /v1/keys/me`.
- Esperado: CONTRACT.md §A defines exactly 7 endpoints; §E describes key handling as frontend-managed ("the key IS the session"), with no self-serve unauthenticated key-mint endpoint in the contract.
- Observado: production serves a self-serve `POST /v1/keys` that per its own OpenAPI description requires **no auth** — anyone can mint FREE API keys at will (each carrying a 50 req/mo quota), an unbounded key-creation / quota-abuse surface. (NOTE: not exercised — creating keys is a write and outside this read-only smoke test; existence and no-auth contract confirmed from the served schema only.)
- Plain impact: The deployed API has 2–3 endpoints the contract never authorized, including an open, unauthenticated key-minting endpoint that lets anyone create usable API keys — an abuse/rate-limit-bypass risk and a contract divergence the owner should confirm or lock down.
- (resolvedor) Correção: <preenchido pelo resolvedor ao corrigir>
