# F-400 — bbi-web logou "Failed with result 'exit-code'" durante o restart do deploy (auto-recuperado)

## F-400 | categoria: performance | severidade: baixa | status: aberto
- Tela: journalctl -u bbi-web (VPS worbita-dados) — linha `set 06 02:11:31 worbita-dados systemd[1]: bbi-web.service: Failed with result 'exit-code'.` seguida imediatamente por `Started` + `✓ Ready in 281ms`
- Passos: 1) ssh worbita-dados 2) journalctl -u bbi-web --no-pager -n 8
- Esperado: restart do deploy sem instância anterior saindo com exit-code de erro.
- Observado: a instância anterior do bbi-web terminou com `Failed with result 'exit-code'` no momento do restart (02:11:31); a nova subiu limpa 0ms depois e está `active (running)`, `Ready`. Estado ATUAL: saudável. Artefato transitório do deploy, sem impacto no serviço agora.
- Impacto (plain): nenhum no momento — o site subiu e responde; só vale a olhada humana pra confirmar que o exit não-zero foi só o encerramento do processo antigo, não um crash recorrente.
- (resolvedor) Correção: <pendente>

## Re-review 2026-09-06 (pós-redeploy) — status: verificado
- Após o redeploy pós-fix, bbi-web está `active (running)` (ActiveEnter novo), sem recorrência do `Failed with result 'exit-code'` na janela desde o redeploy (journal: 0 erros de exit). Era artefato transitório do restart anterior, não crash recorrente. Confirmado saudável — sem ação necessária.
