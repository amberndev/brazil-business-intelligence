# F-104 — No rate-limit HTTP headers; 429 lacks Retry-After

## F-104 | categoria: usabilidade | severidade: baixa | status: aberto
- Tela: live API over HTTPS. Header scan (`curl -D -`) on authed responses, health, and the 429 throttle response.
- Passos:
  1) Inspect response headers of `/v1/health`, `/v1/market/overview` (authed 403), and the 429 from POST `/v1/keys` throttle.
- Esperado (task asked to verify rate-limit response headers): standard `X-RateLimit-Limit` / `X-RateLimit-Remaining` / `X-RateLimit-Reset`, and `Retry-After` on 429.
- Observado: none of those headers are present on any response. Remaining quota is only exposed inside the JSON body as `meta.requests_remaining`. The 429 `create_throttled` response carries no `Retry-After` header (only a prose "Retry in 1 hour" in the message).
- Note: functionally the throttle (429) and quota work; this is a standards/DX gap, not a break. Programmatic clients cannot read quota/backoff from headers.
- (resolvedor) Correção: <pendente> — optional: emit `X-RateLimit-*` and `Retry-After` headers.


---

# Re-teste QA — 2026-09-06 17:24 (board solto em E:\Overclok\TASK, consolidado aqui)

# F-104 — X-RateLimit-* headers on authenticated responses

## F-104 | categoria: funcional | severidade: média | status: verificado
- Tela: HTTPS curl response headers (h_company_petrobras.txt, s3_hdr.txt, del_hdr.txt)
- Passos: 1) Authenticated GET /v1/company/{cnpj} 2) inspect response headers
- Esperado: F-104 claimed FIXED — all three X-RateLimit-* headers present on authenticated responses.
- Observado: **FIXED (verified).** All three present and well-formed on authenticated **success** responses:
  - `X-RateLimit-Limit: 50`
  - `X-RateLimit-Remaining: 47` (decrements across calls: 50→47→45 observed)
  - `X-RateLimit-Reset: 2026-10-01T03:00:00+00:00` (RFC-3339 UTC) ✓
  - Present on 200 (company, successful search) and on 204 (DELETE /v1/keys/me).
- Notes / low-severity observations (not blockers):
  - Headers are ABSENT on 403 `plan_forbidden` responses (compliance/shareholders/batch/market/csv all 0/3). Contract line 76 says "every authenticated response (success and 429)"; a 403 is authenticated but the contract only enumerates success+429, so this is arguably compliant — flagged for owner confirmation. See [[F-108]].
  - Header source inconsistency: reads report decrementing Remaining (47, 45) while DELETE reported Remaining=50 — the value likely comes from the Redis limiter on reads vs the DB row on delete. Cosmetic; worth aligning.
  - 429 path not exercised (would require exhausting the 50-req/mo quota); header-on-429 not independently confirmed this pass.
- (resolvedor) Correção: n/a — verified fixed.
